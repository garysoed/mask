import { RgbColor } from '@gs-tools/color';

const GREY = RgbColor.newInstance(128, 142, 154);
const RED = RgbColor.newInstance(220, 28, 0);
const ORANGE = RgbColor.newInstance(255, 120, 1);
const AMBER = RgbColor.newInstance(255, 193, 1);
const YELLOW = RgbColor.newInstance(255, 251, 1);
const LIME = RgbColor.newInstance(126, 225, 32);
const GREEN = RgbColor.newInstance(0, 192, 22);
const TEAL = RgbColor.newInstance(0, 192, 148);
const CYAN = RgbColor.newInstance(32, 222, 224);
const AZURE = RgbColor.newInstance(32, 154, 224);
const BLUE = RgbColor.newInstance(28, 67, 195);
const VIOLET = RgbColor.newInstance(65, 24, 172);
const PURPLE = RgbColor.newInstance(123, 28, 196);
const MAGENTA = RgbColor.newInstance(167, 29, 197);
const PINK = RgbColor.newInstance(220, 0, 131);
const BROWN = RgbColor.newInstance(136, 64, 0);

export const Palette = {
  AMBER,
  AZURE,
  BLUE,
  BROWN,
  CYAN,
  GREEN,
  GREY,
  LIME,
  MAGENTA,
  ORANGE,
  PINK,
  PURPLE,
  RED,
  TEAL,
  VIOLET,
  YELLOW,
};
